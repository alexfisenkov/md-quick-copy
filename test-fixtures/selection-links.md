# Selection And Links Test

This line contains the selectable target phrase for partial mouse copy.

Open [Example Domain](https://example.com/) or the bare link https://example.com/path?q=quick-look.

1. Ordered list links keep their number: [numbered docs](https://example.com/docs).

> Blockquote links also stay clickable: [quoted link](https://example.com/quote).

```swift
let copied = "code block still copies as a whole"
print(copied)
```
